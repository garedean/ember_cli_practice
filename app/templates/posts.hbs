<h1>Posts</h1>

<ul class="list-unstyled">
  {{#each post in model}}
    <li>
      {{#link-to 'post' post.id}}
        {{post.title}}
        <span class="muted">
          by {{post.author.name}}
        </span>
      {{/link-to}}
    </li>
  {{/each}}
</ul>
